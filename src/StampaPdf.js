//funzione per stampare i dati nel pdf
import { degrees, PDFDocument, rgb, StandardFonts } from 'pdf-lib';
import { saveAs } from 'file-saver';


 export const stampaPdf = () => {
  console.log('hai chiamato la funzione di stampa!!!')
}

